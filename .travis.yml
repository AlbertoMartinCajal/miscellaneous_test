language: c

compiler: gcc

sudo: required

before_install:
  - sudo apt-get update
  - sudo apt-get install -y libxml2-dev
  - chmod +x install-sw.sh

install: 
  - bash ./install-sw.sh

script: 
  - bash ./check_dependencies.sh
